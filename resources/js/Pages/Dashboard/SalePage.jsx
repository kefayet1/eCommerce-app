import DashboardLayout from "@/Layouts/DashboardLayout";

const SalePage = () => {
  return (
    <div>SalePage</div>
  )
}
SalePage.layout = (page) => <DashboardLayout children={page} />;
export default SalePage